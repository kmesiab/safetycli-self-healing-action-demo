# Intentionally vulnerable packages for testing SafetyCLI Self-Healing Action
# These are old versions with known CVEs

django==2.2.0
flask==0.12.0
requests==2.6.0
pillow==6.0.0
urllib3==1.24.0
jinja2==2.10.0
pyyaml==3.12
